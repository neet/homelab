---
all:
  children:
    nas_servers:
      hosts:
        mitaka-storage-01:
          ansible_host: 192.168.1.100  # NASサーバーのIPアドレスに変更してください
          ansible_user: neet
          ansible_become: yes
